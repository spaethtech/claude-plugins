---
name: remote-enable
description: Enable auto-start on boot for the current project by adding an @reboot cron entry
disable-model-invocation: true
allowed-tools: Bash
---

Enable auto-start on boot for the current project by adding an @reboot cron entry.

Run the following bash command:
```bash
${CLAUDE_PLUGIN_ROOT}/scripts/claude-remote enable
```

Report the output to the user.
