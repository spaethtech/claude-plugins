---
name: remote-list
description: List all running Claude Code remote-control tmux sessions across all projects
disable-model-invocation: true
allowed-tools: Bash
---

List all running Claude Code remote-control tmux sessions across all projects.

Run the following bash command:
```bash
${CLAUDE_PLUGIN_ROOT}/scripts/claude-remote list
```

Report the output to the user.
